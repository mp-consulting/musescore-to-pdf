# frozen_string_literal: true

source 'https://rubygems.org'

# Runtime dependency of scripts/pdf_to_score.rb (bundled gem since Ruby 3.0).
gem 'rexml', '~> 3.3'

group :development, :test do
  gem 'rspec', '~> 3.13'
  gem 'rubocop', '~> 1.84', require: false
end
